[project]
name = "py-esol"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.22.2",
    "pytest>=9.1.1",
]
